# Docs Assist terminology registry for this repo.
#
# Canonical terms and the variants to avoid. Machine-checkable pairs only;
# judgment-based guidance lives in style.md, which wins on conflict.

# Product name in prose. The plugin id stays docs-assist in code, commands,
# and file paths.
Docs Assist = DocsAssist, docs assist

# One word.
subagent = sub-agent, sub agent

# Two words in prose. The frontmatter field stays content-type.
content type = contenttype

# Full name on first use in a doc.
The Good Docs Project = Good Docs project, gooddocs
