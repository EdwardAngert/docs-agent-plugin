# Docs Assist terminology registry.
#
# Canonical product terms and the variants to avoid. Docs Assist writes with
# the canonical terms and flags the variants during audits, so the same
# concept never appears under different names across the docs.
#
# Format: canonical = variant, variant. Comments carry nuance the pair cannot.
# Edit freely; the plugin respects what you set and adds terms it introduces.
#
# Examples (replace with your product's terms):
# Acme Deploy = acme deploy, AcmeDeploy
# workspace = project, environment
