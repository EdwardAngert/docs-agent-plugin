{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; case \"$f\" in *.md|*.mdx) { [ -n \"$f\" ] && [ -f .markdownlint.jsonc ] && npx --yes markdownlint-cli2 \"$f\" >&2; } || true ;; esac; }"
          }
        ]
      }
    ]
  }
}
